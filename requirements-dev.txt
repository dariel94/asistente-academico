# Dependencias adicionales para correr la suite de tests unitarios
# (ver tests/ y pytest.ini). Instalar con:
#   pip install -r requirements-dev.txt

-r requirements.txt

pytest==8.3.3
pytest-asyncio==0.24.0
respx==0.21.1
